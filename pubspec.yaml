name: footballtraining
description: "Professional Football Training Management App - Streamline team management, training sessions, attendance tracking, and reporting for football clubs."
publish_to: 'none'

# App version: Major.Minor.Patch+Build
# Update this for each release
version: 1.0.0+1

environment:
  sdk: ^3.3.0

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  flutter_localizations: 
    sdk: flutter
    

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  flutter_launcher_icons: ^0.14.3
  firebase_core: ^4.10.0
  firebase_auth: ^6.5.2
  cloud_firestore: ^6.5.0
  google_fonts: ^8.1.0
  http: ^1.6.0
  image_picker: ^1.1.2
  intl: ^0.20.2
  emailjs: ^4.0.0
  shared_preferences: ^2.5.3
  google_sign_in: ^7.2.0
  cloud_functions: ^6.3.2
  
  # Production-Grade Architecture & State Management
  flutter_bloc: ^9.1.1
  equatable: ^2.0.5
  
  # Environment & Configuration
  flutter_dotenv: ^6.0.1
  
  # Offline Support & Caching
  cached_network_image: ^3.4.1
  
  # Logging & Error Handling
  logger: ^2.4.0
  firebase_crashlytics: ^5.2.3
  firebase_analytics: ^12.4.2
  html: ^0.15.6
  flutter_gen: ^5.14.1
  
  # Enhanced UX & Animations (Optional - comment out if build issues)
  # flutter_animate: ^4.5.0
  # lottie: ^3.1.2
  # shimmer: ^3.0.0
  
  # Notifications (Optional - comment out if build issues)
  # firebase_messaging: ^15.1.3
  # flutter_local_notifications: ^17.2.3
  
  # Form Validation & Utils
  # formz: ^0.7.0
  # freezed_annotation: ^2.4.4
  # json_annotation: ^4.9.0
  
  

flutter_icons:
  android: true
  ios: true
  image_path: "icon/icon.png"
  adaptive_icon_background: "#ffffff"
  adaptive_icon_foreground: "icon/icon1.png"
  remove_alpha_ios: true
  

dev_dependencies:
  flutter_test:
    sdk: flutter
  
  # Code Generation & Build Tools
  build_runner: ^2.4.13
  freezed: ^3.2.5
  json_serializable: ^6.8.0
  
  # Testing & Quality Assurance
  flutter_lints: ^6.0.0
  mockito: ^5.4.4
  bloc_test: ^10.0.0
  integration_test:
    sdk: flutter
  
  # Production Tools
  pdf: ^3.11.1
  printing: ^5.13.4

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.

flutter_launcher_icons:
  android: true
  ios: true
  image_path: "icon/icon.png"
  adaptive_icon_background: "#ffffff"
  adaptive_icon_foreground: "icon/icon1.png"
  remove_alpha_ios: true

flutter:
  generate: true

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true
  

  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/images/
    - icon/
    - .env
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
